/**
 * Power
 * @param {number} m base
 * @param {number} n index
 * @returns {number}
 */
let m = 1;
let n = 1;
let p = 2;
let q = 7;
function sum(m, n) {
  return (m + n);
}
alert(sum(m, n));
alert(sum(p, q));
